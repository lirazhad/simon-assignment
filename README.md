# simon-assignment
$ yarn

# start server
$ cd server
$ npm install
$ npm run start

server will listen to localhost port 3030

# run on ios
$ cd ios
$ bundle install
$ pod install
$ cd..
$ yarn ios

# run on android
$ yarn android
